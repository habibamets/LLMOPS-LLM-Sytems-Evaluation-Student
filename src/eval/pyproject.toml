[project]
name = "llm-eval-chapter5"
version = "0.1.0"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "pandas==3.0.0",
    "evidently==0.7.2",
    "requests==2.32.5"
]

[dependency-groups]
dev = []