[project]
name = "llm-eval-chapter1"
version = "0.1.0"
description = "Chapitre 1 - Setup Evaluation"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "pandas==3.0.0",
    "evidently==0.7.2",
]

[dependency-groups]
dev = []