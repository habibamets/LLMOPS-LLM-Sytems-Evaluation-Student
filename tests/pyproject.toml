[project]
name = "ragops-e2e-tests"
version = "0.1.0"
dependencies = [
    "pytest==9.0.2",
    "testcontainers[compose]==4.14.2",
    "pandas==3.0.1",
    "requests==2.32.5",
    "evidently==0.7.2",
    "openai==2.29.0",
    "pypdf==4.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]